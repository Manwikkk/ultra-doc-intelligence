streamlit>=1.32.0
requests>=2.31.0

# Embedding model (BAAI/bge-small-en) — runs in Streamlit to keep backend lightweight
sentence-transformers>=2.7.0

# Document parsing (replicated from backend so Streamlit can chunk locally)
PyMuPDF>=1.23.0
pdfplumber>=0.11.0
python-docx>=1.1.0
